# Supabase + Next.js 在庫管理システム構築仕様書（15品目対応版）

## 1. 開発スタック
- **Frontend**: Next.js 14+ (App Router)
- **Database & Auth**: Supabase (PostgreSQL)
- **Language**: TypeScript
- **Styling**: Tailwind CSS + shadcn/ui
- **Deployment**: Cloudflare (Cloudflare Pages)

## 2. データベース設計 (Supabase SQL)
Supabaseの「SQL Editor」に貼り付けて実行してください。

```sql
-- 1. 在庫マスターテーブル
CREATE TABLE inventory (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL UNIQUE,
  current_stock NUMERIC DEFAULT 0,
  unit TEXT NOT NULL,
  alert_threshold NUMERIC DEFAULT 20, -- 在庫少のアラート閾値
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 2. 卸先店舗テーブル
CREATE TABLE shops (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL UNIQUE,
  address TEXT
);

-- 3. 入出庫履歴テーブル
CREATE TABLE transactions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  item_id UUID REFERENCES inventory(id),
  shop_id UUID REFERENCES shops(id), -- 入庫の場合はNULL
  type TEXT CHECK (type IN ('IN', 'OUT')),
  quantity NUMERIC NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- デモデータの挿入 (15品目)
INSERT INTO inventory (name, current_stock, unit, alert_threshold) VALUES 
('カボチャ', 150, 'kg', 30), ('ジャガイモ', 300, 'kg', 50), ('ナス', 40, 'kg', 15), 
('生姜', 20, 'kg', 5), ('ピーマン', 15, 'kg', 5), ('オクラ', 10, '袋', 5), 
('四万十セリ', 25, '束', 10), ('里芋', 80, 'kg', 20), ('甘長とうがらし', 12, '袋', 5), 
('米茄子', 35, 'kg', 10), ('キャベツ', 60, '個', 15), ('ブロッコリー', 25, '個', 8),
('ミニトマト', 100, 'パック', 20), ('キュウリ', 45, 'kg', 10), ('白菜', 50, '個', 10);

-- 店舗データの挿入 (5店舗)
INSERT INTO shops (name) VALUES 
('道の駅あぐり窪川'), ('四万十産直市場'), ('地元スーパーA'), ('地元スーパーB'), ('市内レストラン');
```

## 3. UI/UX 機能要件

### A. 在庫ダッシュボード (メイン)
- **15品目のグリッド表示**: 画面をスクロールせずに主要品目が見えるレイアウト。
- **ダイナミック・アラート**:
  - `current_stock < alert_threshold` なら赤色（品薄）。
  - 在庫が豊富な場合は青色（営業チャンス）。
- **リアルタイム反映**: Supabase Realtimeを有効にし、他者が更新したら即座に画面が変わる。

### B. 入出庫操作 (現場用)
- **大きなボタン設計**: 現場での「押し間違い」を防ぐサイズ。
- **2ステップ入力**: 「品目を選ぶ」→「数量を入れる（出庫なら店も選ぶ）」だけのシンプルUI。

### C. 履歴表示 (営業・管理用)
- **最新トランザクション**: 直近30件程度の動きを表示。
- **店舗別フィルタ**: 「道の駅には最近何をどれだけ出したか」がすぐ分かる。

## 4. AI（Cursor / Claude / GPT）へのプロンプト

**指示内容:**
Next.js 14 (App Router) と Supabase を使用して、企業組合向けの在庫管理デモアプリを作成してください。

**前提条件:**
- 上記のSQL仕様に従って、`inventory`, `shops`, `transactions` の3つのテーブルを扱う。
- 野菜は15品目あり、それぞれに在庫閾値（`alert_threshold`）を設定する。

**実装詳細:**
- **Client SDK**: `@supabase/supabase-js` を使用し、データフェッチとリアルタイム更新を実装して。
- **UI**: `shadcn/ui` の Card, Button, Input, Select, Dialog（モーダル）コンポーネントを使用。
- **機能**:
  - ダッシュボードで15品目の在庫を一覧表示。
  - 入庫（IN）と出庫（OUT）の登録フォーム。出庫時は `shops` から店舗を選択。
  - 登録完了時、`inventory` テーブルの数値更新と `transactions` への履歴挿入を同時に行う（RPCまたはPromise.allを使用）。
  - 在庫数が閾値を下回っている品目を強調表示して。

**デザイン:**
四万十の農業をイメージした、清潔感のある緑（Emerald/Green）をアクセントカラーにしたモダンなデザインにしてください。
